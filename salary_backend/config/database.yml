default: &default
  adapter: postgresql
  encoding: unicode

development:
  <<: *default
  database: salary_dev

test:
  <<: *default
  database: salary_test

production:
  adapter: postgresql
  url: <%= ENV["DATABASE_URL"] %>